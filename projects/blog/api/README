## 開発環境のセットアップ

### 必要なもの
- Docker
- Colima（Mac）

### 起動方法
```bash
colima start
docker compose up -d
docker compose exec app bash
```

### 開発
コンテナ内で作業します。

```bash
npm run dev  # 開発サーバー起動
```

### 動作確認
```bash
curl http://localhost:3000/health
```
