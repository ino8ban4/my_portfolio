services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - .:/workspace
      - ~/.config/nvim:/root/.config/nvim
      - node_modules:/workspace/node_modules
    stdin_open: true
    tty: true

volumes:
  node_modules:
