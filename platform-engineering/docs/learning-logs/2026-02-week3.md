# 2026年2月 Week3 学習ログ

## 2/17（火）- Issue #4: ConfigMap/Secret実践

### 学習時間
- 開始: 19:00頃
- 終了: （記入）
- 実時間: 約X時間

### 実施内容
1. **ConfigMap作成**
   - express-configmap.yaml作成
   - アプリケーション設定の外部化（APP_NAME, LOG_LEVEL, NODE_ENV等）
   - `kubectl apply/get/describe`コマンド習得

2. **Secret作成**
   - express-secret.yaml作成
   - 機密情報の管理（DB_USER, DB_PASSWORD等）
   - Base64エンコーディングとstringDataの違い理解

3. **Deploymentへの適用**
   - express-deployment.yaml修正
   - `env.valueFrom.configMapKeyRef`/`secretKeyRef`構文習得
   - ローリングアップデート動作確認

### 学んだ概念
- ConfigMap vs Secret の使い分け
- リソースタイプ指定の必要性（同名リソースが共存可能）
- kubectl コマンド（apply, get, describe, exec）
- YAMLインデントの厳格性
- ローリングアップデートの仕組み
- 環境変数注入の確認方法

### 成果物
- `k8s-manifests/express-configmap.yaml`
- `k8s-manifests/express-secret.yaml`
- `k8s-manifests/express-deployment.yaml`（ConfigMap/Secret参照版）

### 次回予定
- Volume/PersistentVolumeの学習
- 複数環境（dev/staging/prod）管理パターン

### メモ・気づき
- Base64は暗号化ではなくエンコード（セキュリティ的には平文同然）
- 実務ではSealed SecretsやExternal Secrets Operatorを使う
- YAMLのインデントエラーに注意
- kubectl execで環境変数確認が便利

## 2/19（木）- Issue #4: Volume基礎実践

### 学習時間
- 開始: 20:48
- 終了: 21:50
- 実時間: 約1時間

### 実施内容
1. **emptyDir実践**
   - マルチコンテナPod作成（writer/reader）
   - コンテナ間でのVolume共有確認
   - Pod削除でデータが消えることを確認

2. **PersistentVolume概念学習**
   - PV/PVCの関係性理解
   - postgres-pv.yaml作成（未適用）

### 学んだ概念
- emptyDirとPersistentVolumeの違い
- マルチコンテナPod（サイドカーパターン）
- kubectl logsとkubectl execの違い
- BusyBoxイメージの用途
- Volume種類（emptyDir, hostPath, PV）

### 成果物
- `k8s-manifests/volume-emptydir-demo.yaml`
- `k8s-manifests/postgres-pv.yaml`（作成のみ）

### 次回予定
- PV/PVC適用
- PostgreSQLでデータ永続化確認
- 複数環境管理

### メモ・気づき
- Podは一時的な存在、データ永続化にはPVが必要
- kubectl logsは標準出力をキャプチャ、ファイル直接読むにはkubectl exec
- YAMLファイル作成 ≠ クラスタへの適用（apply必須）

---

## 2/21（土）- Issue #4: PV/PVC実践・複数環境管理

### 学習時間
- 開始: （記入）
- 終了: （記入）
- 実時間: 約2時間

### 実施内容
1. **PersistentVolume/PVC実践**
   - postgres-pv.yaml適用
   - postgres-pvc.yaml作成・適用
   - PVとPVCの自動マッチング確認

2. **PostgreSQLでデータ永続化検証**
   - postgres-deployment.yaml作成
   - PVCをVolumeとしてマウント
   - データ書き込み→Pod削除→データ残存確認（成功）

3. **複数環境管理**
   - express-configmap-dev.yaml作成
   - express-configmap-prod.yaml作成
   - express-deployment-dev.yaml作成
   - envFromでConfigMap一括取得
   - dev/prod環境の設定値の違いを確認

### 学んだ概念
- PV/PVCのマッチング条件（storageClassName, accessModes, capacity）
- 複数PV/PVCを独立させる方法（storageClassNameを分ける）
- hostPathの動作（minikubeでは論理的容量、クラウドでは物理的確保）
- データ保存場所の階層（Mac → minikube VM → /mnt/data/postgres）
- envFromによるConfigMap一括取得
- 環境切り替えの仕組み（configMapRef.nameを変えるだけ）
- kubectl explainでYAML仕様をローカル確認
- Deploymentの役割（複数Pod管理、docker-composeに近い）

### 成果物
- `k8s-manifests/postgres-pv.yaml`
- `k8s-manifests/postgres-pvc.yaml`
- `k8s-manifests/postgres-deployment.yaml`
- `k8s-manifests/express-configmap-dev.yaml`
- `k8s-manifests/express-configmap-prod.yaml`
- `k8s-manifests/express-deployment-dev.yaml`

### Issue #4完了
- [x] ConfigMap/Secretの理解と実践
- [x] 環境変数の外部化
- [x] Volume/PersistentVolumeの基礎
- [x] 複数環境（dev/staging/prod）の管理

### 次回予定
- Issue #5: Kubernetesマニフェスト作成
- Kustomizeの基礎
- マニフェストのテンプレート化

### メモ・気づき
- PVは「物件」、PVCは「入居申込書」、Podは「入居者」
- ConfigMapとDeploymentの紐付けは`configMapRef.name`の1行のみ
- Kubernetesリソース8種類習得（Pod, Deployment, ReplicaSet, Service, ConfigMap, Secret, PV, PVC）
