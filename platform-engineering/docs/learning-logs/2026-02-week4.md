# 2026年2月 Week4 学習ログ

## 2/25（火）- Issue #5: Kustomize実践

### 学習時間
- 開始: 20:48
- 終了: 22:50
- 実時間: 約2時間

### 実施内容
1. **環境復旧**
   - Mac再起動後のColima/minikube復旧
   - リソース状態確認

2. **Colima/minikube/kubectlの関係理解**
   - Colimaの役割（Dockerランタイム提供）
   - minikubeの正体（クラスタ構築ツール）
   - kubectlの汎用性（どのK8sでも共通）

3. **Kustomize基礎**
   - base/overlays構成の理解
   - namePrefix自動適用
   - replicas上書き
   - patchesによる部分修正

4. **Kustomize実装**
   - base構成作成（deployment, service, kustomization.yaml）
   - overlays/dev作成（replicas: 1, nodePort: 30300）
   - overlays/prod作成（replicas: 3, nodePort: 30301）
   - 環境別デプロイ成功

### 学んだ概念
- Colima（Docker環境）→ minikube（K8sクラスタ）→ kubectl（操作）の関係
- minikubeは「ツール」であり「ランタイム」ではない
- kubectlはどのKubernetesクラスタでも使える標準CLI
- Kustomizeの必要性（ファイル重複削減、環境差分管理）
- base/overlays設計パターン
- kustomization.yamlの役割（ファイル名固定）
- namePrefix自動付与の仕組み
- patchesによる部分的な上書き
- kubectl kustomizeとkubectl apply -kの違い
- Serviceのポート（port, targetPort, nodePort）の役割
- selectorとlabelsによるグループ化

### 成果物
- `k8s-manifests/kustomize/base/deployment.yaml`
- `k8s-manifests/kustomize/base/service.yaml`
- `k8s-manifests/kustomize/base/kustomization.yaml`
- `k8s-manifests/kustomize/overlays/dev/configmap.yaml`
- `k8s-manifests/kustomize/overlays/dev/kustomization.yaml`
- `k8s-manifests/kustomize/overlays/prod/configmap.yaml`
- `k8s-manifests/kustomize/overlays/prod/service-patch.yaml`
- `k8s-manifests/kustomize/overlays/prod/kustomization.yaml`

### Issue #5完了
- [x] YAML文法の深掘り（既習のためスキップ）
- [x] Kustomizeの基礎
- [x] マニフェストのテンプレート化
- [x] 環境別マニフェスト管理（base + overlays）

### 次回予定
- 学習ログ記録・Gitコミット
- チートシート作成（Kubernetesマニフェストテンプレート集）

### メモ・気づき
- Kustomizeで共通部分を1箇所管理、環境差分だけoverlaysに記述
- namePrefixで自動的に環境名が付与される
- patchesで柔軟に設定上書き可能
- Issue #4（見積8h）→4.5h、Issue #5（見積8h）→2h、効率的に完了
- kubectl explainでローカルでYAML仕様確認可能
- Kubernetesリソース8種類習得済み

## 3/1（日）- Kubernetesマニフェストテンプレート集作成

### 学習時間
- 開始: （記入）
- 終了: （記入）
- 実時間: 約2.5時間

### 実施内容
1. **テンプレート作成（10ファイル）**
   - deployment.yaml（Pod管理）
   - service.yaml（ネットワーク）
   - configmap.yaml（設定データ）
   - secret.yaml（機密情報）
   - persistentvolume.yaml（ストレージ実体）
   - persistentvolumeclaim.yaml（ストレージ要求）
   - kustomization.yaml（Kustomize設定）
   - namespace.yaml（Namespace定義）
   - ingress.yaml（外部公開・ルーティング）
   - README.md（使い方ガイド）

2. **各テンプレートに詳細コメント追加**
   - 用途説明
   - 必須フィールド
   - 使用方法・例
   - ベストプラクティス
   - トラブルシューティング

3. **README作成**
   - ファイル一覧・必須度
   - 置換項目ガイド
   - ユースケース別手順（シンプルアプリ/DB含む/Kustomize）
   - ベストプラクティス
   - トラブルシューティング

### 学んだ概念
- namespace.yamlの使い分け（Kustomize vs 手動管理）
- Namespaceの紐付け仕組み（metadata.namespace）
- PV/PVCの実務での位置づけ（マネージドDB vs 自前管理）
- Ingressの2段階ルーティング（Ingress→Service, Service→Pod）
- Base64エンコーディングの目的（暗号化ではなくデータ形式変換）
- persistentVolumeReclaimPolicy（Retain/Delete/Recycle）
- ConfigMap vs Secretの使い分け
- Service ports種類（port/targetPort/nodePort）
- マルチテナント構成とセキュリティリスク
- Namespace分離の限界と対策レベル

### 成果物
- `k8s-manifests/templates/` ディレクトリ
  - 10種類のマニフェストテンプレート
  - 詳細コメント・使用例付き
  - README.md（包括的なガイド）

### 次回予定
- 2月振り返り・3月計画立案
- Issue #6以降の準備

### メモ・気づき
- namespace.yamlはKustomize使用時は基本不要（ResourceQuota等とセットの場合は有用）
- PV/PVCは実務ではニッチな用途（マネージドサービス主流）
- Ingressは未実践（Issue #6以降で実施予定）
- テンプレート集は今後の開発で活用可能な資産
- セキュリティ対策（RBAC, NetworkPolicy等）は段階的に学習予定
