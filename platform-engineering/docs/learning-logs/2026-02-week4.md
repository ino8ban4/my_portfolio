# 2026年2月 Week4 学習ログ

## 2/25（火）- Issue #5: Kustomize実践

### 学習時間
- 開始: 20:48
- 終了: 22:50
- 実時間: 約2時間

### 実施内容
1. **環境復旧**
   - Mac再起動後のColima/minikube復旧
   - リソース状態確認

2. **Colima/minikube/kubectlの関係理解**
   - Colimaの役割（Dockerランタイム提供）
   - minikubeの正体（クラスタ構築ツール）
   - kubectlの汎用性（どのK8sでも共通）

3. **Kustomize基礎**
   - base/overlays構成の理解
   - namePrefix自動適用
   - replicas上書き
   - patchesによる部分修正

4. **Kustomize実装**
   - base構成作成（deployment, service, kustomization.yaml）
   - overlays/dev作成（replicas: 1, nodePort: 30300）
   - overlays/prod作成（replicas: 3, nodePort: 30301）
   - 環境別デプロイ成功

### 学んだ概念
- Colima（Docker環境）→ minikube（K8sクラスタ）→ kubectl（操作）の関係
- minikubeは「ツール」であり「ランタイム」ではない
- kubectlはどのKubernetesクラスタでも使える標準CLI
- Kustomizeの必要性（ファイル重複削減、環境差分管理）
- base/overlays設計パターン
- kustomization.yamlの役割（ファイル名固定）
- namePrefix自動付与の仕組み
- patchesによる部分的な上書き
- kubectl kustomizeとkubectl apply -kの違い
- Serviceのポート（port, targetPort, nodePort）の役割
- selectorとlabelsによるグループ化

### 成果物
- `k8s-manifests/kustomize/base/deployment.yaml`
- `k8s-manifests/kustomize/base/service.yaml`
- `k8s-manifests/kustomize/base/kustomization.yaml`
- `k8s-manifests/kustomize/overlays/dev/configmap.yaml`
- `k8s-manifests/kustomize/overlays/dev/kustomization.yaml`
- `k8s-manifests/kustomize/overlays/prod/configmap.yaml`
- `k8s-manifests/kustomize/overlays/prod/service-patch.yaml`
- `k8s-manifests/kustomize/overlays/prod/kustomization.yaml`

### Issue #5完了
- [x] YAML文法の深掘り（既習のためスキップ）
- [x] Kustomizeの基礎
- [x] マニフェストのテンプレート化
- [x] 環境別マニフェスト管理（base + overlays）

### 次回予定
- 学習ログ記録・Gitコミット
- チートシート作成（Kubernetesマニフェストテンプレート集）

### メモ・気づき
- Kustomizeで共通部分を1箇所管理、環境差分だけoverlaysに記述
- namePrefixで自動的に環境名が付与される
- patchesで柔軟に設定上書き可能
- Issue #4（見積8h）→4.5h、Issue #5（見積8h）→2h、効率的に完了
- kubectl explainでローカルでYAML仕様確認可能
- Kubernetesリソース8種類習得済み
